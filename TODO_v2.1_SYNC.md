# TODO v2.1: Инкрементальный сбор и синхронизация с GitHub

**Дата:** 2026-02-08
**Цель:** Система регулярной выгрузки новых операций с дедупликацией и хранением в приватном GitHub-репозитории
**Результат:** `universal_collector_v2.1.0.user.js`
**Статус:** ✅ РЕАЛИЗОВАНО (код написан, тестирование в процессе)

---

## Принятые решения

| Параметр | Решение |
|----------|---------|
| Хранилище | Приватный GitHub-репозиторий |
| Структура файлов | `{siteId}/{clientId}_{alias}.json` |
| Идентификация аккаунта | `siteId + clientId` (автоматически) + `alias` (вводится один раз) |
| Токен GitHub | Хранится в `GM_setValue`, вводится при первой настройке |
| Merge-логика | Внутри Tampermonkey-скрипта (кнопка "Sync") |
| Обработка дубликатов | По `marker` — перезапись новым состоянием |
| Детали ставок | Загружаются для всех ставок каждый раз |
| Запуск сбора | Вручную |
| Масштаб | 20+ аккаунтов, 2+ сайта |

---

## Фаза 0: Подготовка инфраструктуры

### 0.1. Создать приватный GitHub-репозиторий
- [ ] Создать приватный репозиторий (например, `betting-data`)
- [ ] Создать структуру папок: `fonbet/`, `pari/`
- [ ] Добавить `README.md` с описанием структуры
- [ ] Добавить `.gitignore`

### 0.2. Создать GitHub Personal Access Token (fine-grained)
- [ ] Создать fine-grained PAT с правами:
  - `Contents: Read and write` — только для data-репозитория
- [ ] Проверить работу токена через API (GET/PUT файла)

---

## Фаза 1: Модуль GitHubSync

Новый модуль внутри скрипта для взаимодействия с GitHub API.

### 1.1. Добавить `@grant GM_xmlhttpRequest` в метаданные
- [x] Строка 12: добавить `// @grant GM_xmlhttpRequest`
- [x] Это необходимо для кросс-доменных запросов к `api.github.com` из скрипта на `fon.bet`/`pari.ru`
- [x] Обычный `fetch()` не сработает из-за CORS — GitHub API не разрешает запросы с доменов букмекеров
- [x] `GM_xmlhttpRequest` обходит CORS, т.к. выполняется на уровне расширения Tampermonkey

### 1.2. Создать модуль GitHubSync
- [x] Вставить после ExportModule (после строки ~2959)
- [x] Примерная структура:

```javascript
const GitHubSync = {
    // === Конфигурация ===
    API_BASE: 'https://api.github.com',

    // === Состояние ===
    token: null,          // GitHub PAT
    repoOwner: null,      // владелец репо (username)
    repoName: null,       // имя репо (betting-data)
    accountAlias: null,   // человекочитаемое имя аккаунта
    isSyncing: false,     // флаг процесса синхронизации
    lastSyncResult: null, // результат последней синхронизации

    // === Инициализация ===
    init(),               // загрузить настройки из GM_getValue

    // === Настройки ===
    isConfigured(),       // проверить наличие token + repo
    loadConfig(),         // загрузить из GM_getValue
    saveConfig(config),   // сохранить в GM_setValue

    // === GitHub API ===
    _apiRequest(method, path, body),  // обёртка над GM_xmlhttpRequest
    _getFile(path),                    // GET файл из репо (возвращает content + sha)
    _putFile(path, content, sha, message), // PUT файл (создание/обновление)
    _listDirectory(path),              // GET содержимое директории

    // === Sync логика ===
    sync(),                     // основной метод синхронизации
    _findExistingFile(),        // найти файл аккаунта по clientId в директории siteId/
    _buildFilePath(),           // построить путь: {siteId}/{clientId}_{alias}.json
    _mergeData(remote, local),  // объединить удалённые и локальные данные
    _buildSyncPayload(),        // сформировать данные для загрузки

    // === Управление alias ===
    changeAlias(newAlias),      // изменить alias (переименовать файл в GitHub)
    _renameFile(oldPath, newPath, content, sha), // удалить старый + создать новый файл
    _deleteFile(path, sha),     // DELETE файл из репо

    // === UI интеграция ===
    showSetupDialog(),          // диалог первоначальной на��тройки
    getSyncStatus(),            // статус для отображения в UI
};
```

### 1.3. Реализовать хранение конфигурации синхронизации
- [x] Новые ключи в GM_setValue:
  - `'sync_github_token'` — GitHub PAT
  - `'sync_repo_owner'` — username владельца репо
  - `'sync_repo_name'` — имя репозитория
  - `'sync_account_alias'` — человекочитаемое имя аккаунта
- [x] Метод `loadConfig()`: загрузка всех ключей через `GM_getValue`
- [x] Метод `saveConfig(config)`: сохранение через `GM_setValue`
- [x] Метод `isConfigured()`: проверка что все 4 поля заполнены

### 1.4. Реализовать обёртку GitHub API
- [x] Метод `_apiRequest(method, path, body)`:
  - Использовать `GM_xmlhttpRequest` (не `fetch`!)
  - Заголовки: `Authorization: Bearer {token}`, `Accept: application/vnd.github+json`
  - Возвращать Promise (GM_xmlhttpRequest callback → Promise)
  - Обработка ошибок: 401 (невалидный токен), 404 (файл не найден), 422 (конфликт sha), rate limit
- [x] Метод `_getFile(path)`:
  - `GET /repos/{owner}/{repo}/contents/{path}`
  - Декодирование base64 содержимого
  - Возвращает `{ content: object, sha: string }` или `null` если файл не существует
- [x] Метод `_putFile(path, content, sha, message)`:
  - `PUT /repos/{owner}/{repo}/contents/{path}`
  - Кодирование содержимого в base64
  - `sha` обязателен для обновления существующего файла (защита от перезаписи)
  - `message` — commit message
- [x] Метод `_listDirectory(path)`:
  - `GET /repos/{owner}/{repo}/contents/{path}`
  - Возвращает массив файлов в директории

### 1.5. Реализовать поиск файла аккаунта
- [x] Метод `_findExistingFile()`:
  - Получить `siteId` из `SiteDetector.currentSite.id`
  - Получить `clientId` из `OperationsCollector.sessionParams.clientId`
  - Запросить список файлов в `{siteId}/` через `_listDirectory`
  - Найти файл по паттерну `{clientId}_*.json`
  - Если найден — вернуть имя файла и sha
  - Если не найден — вернуть null (будет создан новый)

### 1.6. Реализовать построение пути файла
- [x] Метод `_buildFilePath()`:
  - Если файл уже существует (найден в 1.5) → использовать существующее имя
  - Если новый файл → `{siteId}/{clientId}_{alias}.json`
  - Alias берётся из `GM_getValue('sync_account_alias')` или запрашивается у пользователя

### 1.7. Реализовать изменение alias (переименование файла)
- [x] Метод `changeAlias(newAlias)`:
  - Валидация нового alias (не пустой, допустимые символы)
  - Найти текущий файл через `_findExistingFile()`
  - Если файл не существует → просто обновить alias в GM_setValue
  - Если файл существует → переименовать:
    1. Скачать содержимое текущего файла (`_getFile`)
    2. Обновить поле `account.alias` в содержимом
    3. Создать новый файл с новым именем (`_putFile` с новым путём)
    4. Удалить старый файл (`_deleteFile` со старым path + sha)
    5. Обновить alias в `GM_setValue('sync_account_alias', newAlias)`
  - Показать результат: "✅ Alias изменён: oldAlias → newAlias"
  - При ошибке: откатить (не удалять старый файл, если новый не создался)
- [x] Метод `_deleteFile(path, sha)`:
  - `DELETE /repos/{owner}/{repo}/contents/{path}`
  - Требует `sha` для подтверждения
  - Commit message: `Rename: {oldAlias} → {newAlias}`

---

## Фаза 2: Логика Merge (объединение данных)

### 2.1. Определить формат хранимого JSON-файла
- [x] Формат файла в GitHub-репозитории:

```javascript
{
    "version": "2.1.0",
    "account": {
        "siteId": "fonbet",
        "siteName": "Fonbet",
        "clientId": 17158121,
        "alias": "Vlad"
    },
    "lastSync": "2026-02-08T14:30:00.000Z",
    "syncHistory": [
        {
            "date": "2026-02-08T14:30:00.000Z",
            "operationsAdded": 45,
            "operationsUpdated": 3,
            "totalAfterSync": 564
        }
    ],
    "summary": {
        "totalOperations": 564,
        "totalGroups": 282,
        "regularBets": 271,
        "fastBets": 0,
        "freebets": 0,
        "deposits": 4,
        "withdrawals": 7,
        "bonus": 0,
        "detailsLoaded": 271,
        "detailsFailed": 0,
        "detailsSkipped": 0
    },
    "bets": [...],
    "fastBets": [...],
    "freebets": [...],
    "finance": {
        "deposits": [...],
        "withdrawals": [...],
        "holds": [...]
    },
    "bonus": [...]
}
```

### 2.2. Реализовать метод `_mergeData(remote, local)`
- [x] Входные данные:
  - `remote` — данные из GitHub (или null если файл новый)
  - `local` — данные текущего сбора (формат ExportModule.exportOperations)
- [x] Логика merge для каждой категории:

#### 2.2.1. Merge ставок (bets)
- [x] Ключ дедупликации: `marker`
- [x] Если marker существует в remote:
  - Перезаписать всю группу новыми данными (статус, детали, операции)
- [x] Если marker новый:
  - Добавить в массив
- [x] Сортировка результата по времени (по убыванию, новые сверху)

#### 2.2.2. Merge быстрых ставок (fastBets)
- [x] Аналогично bets — ключ `marker`, перезапись при совпадении

#### 2.2.3. Merge фрибетов (freebets)
- [x] Аналогично bets — ключ `marker`, перезапись при совпадении

#### 2.2.4. Merge финансовых операций (finance.deposits, finance.withdrawals, finance.holds)
- [x] Ключ дедупликации: `marker`
- [x] Перезапись при совпадении
- [x] Добавление новых

#### 2.2.5. Merge бонусов (bonus)
- [x] Ключ дедупликации: `marker`
- [x] Перезапись при совпадении

### 2.3. Реализовать подсчёт статистики merge
- [x] Подсчитать:
  - `operationsAdded` — количество новых marker'ов (не существовавших в remote)
  - `operationsUpdated` — количество обновлённых marker'ов (существовали, перезаписаны)
  - `operationsUnchanged` — количество marker'ов только в remote (не затронуты)
  - `totalAfterSync` — общее количество операций после merge
- [x] Добавить запись в `syncHistory[]`

### 2.4. Реализовать обновление summary после merge
- [x] Пересчитать все поля summary на основе объединённых данных:
  - `totalOperations` — сумма операций во всех категориях
  - `totalGroups` — количество уникальных marker'ов
  - `regularBets`, `fastBets`, `freebets` — из длин массивов
  - `deposits`, `withdrawals` — из finance
  - `bonus` — из длины массива bonus
  - `detailsLoaded`, `detailsFailed`, `detailsSkipped` — из текущей сессии

---

## Фаза 3: Основной метод sync()

### 3.1. Реализовать метод `sync()`
- [x] Полный flow синхронизации:

```
1. Проверить isConfigured() → если нет → showSetupDialog()
2. Проверить что сбор завершён (OperationsCollector.completed === true)
3. Проверить что детали загружены (BetsDetailsFetcher не в процессе)
4. Установить isSyncing = true
5. Показать прогресс в UI: "Синхронизация... Этап 1/4: Подготовка данных"
6. Собрать локальные данные через ExportModule (без скачивания файла)
7. Показать прогресс: "Этап 2/4: Загрузка данных из GitHub"
8. Скачать удалённый файл через _getFile() (или null если первый sync)
9. Показать прогресс: "Этап 3/4: Объединение данных"
10. Выполнить _mergeData(remote, local)
11. Показать прогресс: "Этап 4/4: Сохранение в GitHub"
12. Загрузить объединённые данные через _putFile()
13. Обновить lastSyncResult
14. Показать результат в UI: "✅ Синхронизация завершена: +N новых, M обновлено"
15. Установить isSyncing = false
```

### 3.2. Обработка ошибок sync()
- [x] 401 Unauthorized → "Невалидный GitHub токен. Проверьте настройки."
- [x] 404 Not Found (репо) → "Репозиторий не найден. Проверьте owner/name."
- [x] 409/422 Conflict (sha mismatch) → повторить GET + merge + PUT (один раз)
- [x] Сетевая ошибка → "Ошибка сети. Проверьте подключение."
- [x] Rate limit → "Превышен лимит GitHub API. Повторите через N минут."
- [x] При любой ошибке: `isSyncing = false`, показать ошибку в UI

### 3.3. Реализовать метод `_buildSyncPayload()`
- [x] Переиспользовать логику из `ExportModule.exportOperations()` (строки 2819-2863)
- [x] Не скачивать файл, а вернуть объект данных
- [x] Добавить поля `account`, `lastSync`, `syncHistory`

---

## Фаза 4: Диалог настройки (первый запуск)

### 4.1. Реализовать `showSetupDialog()`
- [x] Модальное окно поверх страницы (аналогично _createSettingsPanel)
- [x] Поля ввода:
  - **GitHub Token** — `<input type="password">` с кнопкой "показать"
  - **Repository Owner** — `<input type="text" placeholder="username">`
  - **Repository Name** — `<input type="text" placeholder="betting-data">`
  - **Account Alias** — `<input type="text" placeholder="Vlad, Sergey...">`
- [x] Кнопка "Проверить подключение":
  - Выполняет тестовый запрос `GET /repos/{owner}/{repo}`
  - Показывает результат: ✅ Подключено / ❌ Ошибка (с деталями)
- [x] Кнопка "Сохранить":
  - Сохраняет все 4 поля через GM_setValue
  - ��акрывает диалог
- [x] Кнопка "Отмена"

### 4.2. Валидация полей
- [x] Token: не пустой, начинается с `ghp_` или `github_pat_`
- [x] Owner: не пустой, только буквы/цифры/дефис
- [x] Repo: не пустой, только буквы/цифры/дефис/подчёркивание
- [x] Alias: не пустой, только буквы/цифры/подчёркивание (для имени файла)

### 4.3. Автоопределение при первом sync
- [x] Если alias не задан, но token + repo настроены:
  - При первом sync показать prompt для ввода alias
  - Сохранить в GM_setValue

---

## Фаза 5: UI интеграция

### 5.1. Добавить кнопку "Sync" в UIPanel
- [x] Добавить кнопку `🔄 Sync` в панель (рядом с "Экспорт данных")
- [x] В `_getHTML()` (строки 1551-1658): добавить HTML кнопки
- [x] В `_cacheElements()` (строки 2214-2252): закэшировать элемент
- [x] В `_attachEventListeners()` (строки 2257-2287): привязать обработчик
- [x] Обработчик: `GitHubSync.sync()`

### 5.2. Добавить кнопку "⚙️ Sync Settings"
- [x] Кнопка для открытия диалога настройки синхронизации
- [x] Можно добавить в существующую панель настроек (рядом с Export/Fetcher настройками)
- [x] Или отдельная маленькая кнопка рядом с "Sync"

### 5.3. Добавить индикатор статуса синхронизации
- [x] Под кнопкой Sync или в блоке статистики:
  - Если не настроено: `⚠️ Sync не настроен`
  - Если настроено: `📅 Последняя синхронизация: 08.02.2026 14:30`
  - Во время sync: спиннер + текущий этап
  - После успешного sync: `✅ +45 новых, 3 обновлено`
  - При ошибке: `❌ Ошибка: описание`

### 5.4. Состояния кнопки Sync
- [x] **Disabled** — если сбор не завершён или детали ещё загружаются
- [x] **Active** — если сбор завершён и sync не в процессе
- [x] **Loading** — во время синхронизации (спиннер, текст "Syncing...")
- [x] **Success** — после успешной синхронизации (зелёный, через 3 сек. возврат к Active)
- [x] **Error** — при ошибке (красный, с tooltip)

### 5.5. Прогресс синхронизации
- [x] Переиспользовать существующий `showProgress()` (строки 2398-2413)
- [x] Этапы:
  1. "Подготовка данных..." (25%)
  2. "Загрузка из GitHub..." (50%)
  3. "Объединение данных..." (75%)
  4. "Сохранение в GitHub..." (100%)

### 5.6. Добавить пункт "Изменить alias" в меню
- [x] Добавить ссылку/кнопку `✏️ Изменить alias` в панель (в блоке настроек Sync или рядом с индикатором статуса)
- [x] Видна только если sync настроен (isConfigured() === true)
- [x] Показывает текущий alias: `Аккаунт: Vlad [✏️]`
- [x] При клике:
  - Показать prompt/мини-диалог с текущим alias и полем для нового
  - Валидация: не пустой, допустимые символы (буквы/цифры/подчёркивание)
  - Подтверждение: "Переименовать Vlad → Sergey? Файл в GitHub будет переименован."
  - Вызвать `GitHubSync.changeAlias(newAlias)`
  - Показать результат: успех или ошибка
- [x] После успешного переименования — обновить отображение alias в UI

### 5.7. Исправить читаемость существующей панели настроек
- [x] **Заголовок "Настройки"** (верхняя панель): изменить цвет шрифта с чёрного на белый/светлый — чёрный текст не виден на тёмно-сером фоне
- [x] **Верхняя панель** (с заголовком "Настройки"): сделать фон solid (непрозрачным) вместо полупрозрачного
- [x] **Нижняя панель** (с кнопками "Сбросить" и "Сохранить"): сделать фон solid (непрозрачным) вместо полупрозрачного
- [x] **Лейблы чекбоксов** ("Использовать пользовательский префикс файла", "Включать временную метку в имя файла"): изменить цвет шрифта на более светлый/контрастный для читаемости на тёмном фоне
- [x] **Чекбоксы** (toggle-кнопки): изменить стиль самих кнопок, чтобы визуально было очевидно что это интерактивные элементы (более яркий accent-цвет, чёткая граница active/inactive)

---

## Фаза 6: Интеграция настроек Sync в SettingsManager

### 6.1. Расширить SettingsManager
- [x] Добавить секцию `sync` в DEFAULT_SETTINGS (строки 995-1008):

```javascript
sync: {
    githubToken: '',
    repoOwner: '',
    repoName: 'betting-data',
    accountAlias: ''
}
```

- [x] Обновить `loadSettings()` для загрузки sync-настроек
- [x] Обновить `saveSettings()` для сохранения sync-настроек
- [x] Обновить `applySettings()` для применения к GitHubSync

### 6.2. Добавить секцию Sync в панель настроек
- [x] В `_createSettingsPanel()` (строки 2505-2596): добавить блок настроек синхронизации
- [x] Поля: Token (password), Owner, Repo Name, Alias
- [x] Кнопка "Проверить подключение"

---

## Фаза 7: Обновление ExportModule

### 7.1. Рефакторинг ExportModule.exportOperations()
- [x] Выделить логику формирования данных в отдельный метод `_buildExportData()`
- [x] `exportOperations()` вызывает `_buildExportData()` + скачивает файл
- [x] `GitHubSync._buildSyncPayload()` вызывает `_buildExportData()` + добавляет account/syncHistory
- [x] Это избегает дублирования кода между экспортом и синхронизацией

### 7.2. Добавить поле account в _buildExportData()
- [x] Добавить в формируемый объект:

```javascript
account: {
    siteId: SiteDetector.currentSite?.id,
    siteName: SiteDetector.getSiteName(),
    clientId: OperationsCollector.sessionParams?.clientId,
    alias: GitHubSync.accountAlias || ''
}
```

---

## Фаза 8: Обновление метаданных и версии

### 8.1. Обновить Tampermonkey метаданные
- [x] Строка 3: `@name` → `Fonbet & Pari Collector` (без изменений)
- [x] Строка 4: `@version` → `2.1.0`
- [x] Строка 5: `@description` → добавить "с синхронизацией в GitHub"
- [x] Строка 12: добавить `// @grant GM_xmlhttpRequest`
- [x] Добавить `// @connect api.github.com` — разрешение для GM_xmlhttpRequest

### 8.2. Обновить VERSION
- [x] Строка 19: `VERSION = '2.0.0'` → `VERSION = '2.1.0'`

### 8.3. Обновить window.collector
- [x] Строки 2987-3002: добавить `githubSync: GitHubSync` в глобальный объект
- [x] Добавить `sync: () => GitHubSync.sync()` для консольного доступа
- [x] Добавить `changeAlias: (alias) => GitHubSync.changeAlias(alias)` для консольного доступа

---

## Фаза 9: Тестирование

### 9.1. Unit-тесты (через evaluate_script в MCP devtools)
- [ ] GitHubSync.isConfigured() возвращает false до настройки
- [ ] GitHubSync.isConfigured() возвращает true после настройки
- [ ] GitHubSync._buildFilePath() возвращает корректный путь
- [ ] _mergeData с пустым remote = полная копия local
- [ ] _mergeData с remote + local = корректное объединение
- [ ] _mergeData: дубликаты по marker перезаписываются
- [ ] _mergeData: уникальные операции из remote сохраняются
- [ ] _mergeData: уникальные операции из local добавляются
- [ ] Подсчёт statistik merge корректен

### 9.2. Интеграционные тесты
- [ ] Первый sync (файл не существует):
  - Создаёт новый файл в GitHub
  - Имя файла: `{clientId}_{alias}.json`
  - Содержимое: полные данные текущего сбора
- [ ] Повторный sync (файл существует):
  - Скачивает существующий файл
  - Мержит данные
  - Обновляет файл (с правильным sha)
  - syncHistory содержит новую запись
- [ ] Sync с новыми ставками:
  - Старые ставки сохранены
  - Новые ставки добавлены
  - Обновлённые ставки перезаписаны
- [ ] Sync после ошибки (повторный):
  - Работает корректно после предыдущей ошибки

### 9.3. Тесты безопасности
- [ ] Токен не виден в коде скрипта
- [ ] Токен не попадает в экспортируемый JSON
- [ ] Токен не логируется в консоль (даже в DEBUG_MODE)
- [ ] При @updateURL токен сохраняется (GM_setValue не затрагивается)

### 9.4. Тесты для разных аккаунтов
- [ ] Два аккаунта на одном сайте → два разных файла
- [ ] Один человек на двух сайтах → файлы в разных папках
- [ ] Смена alias → файл остаётся тем же (ищем по clientId)

### 9.5. Тесты изменения alias
- [ ] changeAlias() с несуществующим файлом → обновляет только GM_setValue
- [ ] changeAlias() с существующим файлом:
  - Новый файл создан с новым именем `{clientId}_{newAlias}.json`
  - Старый файл удалён
  - Содержимое `account.alias` обновлено внутри JSON
  - Данные ставок не потеряны
- [ ] changeAlias() с невалидным alias (пустой, спецсимволы) → ошибка валидации
- [ ] changeAlias() при сетевой ошибке на DELETE → старый файл не удалён, новый создан (безопасный откат)
- [ ] UI показывает обновлённый alias после переименования

---

## Фаза 10: Документация

### 10.1. Обновить CONTEXT.md
- [x] Добавить описание модуля GitHubSync
- [x] Обновить структуру кода (номера строк)
- [x] Обновить формат экспорта (поля account, syncHistory)
- [x] Обновить консольные команды (collector.sync())
- [x] Обновить версию

### 10.2. Обновить TEST.md
- [x] Добавить тесты для GitHubSync
- [x] Добавить тесты для merge-логики
- [x] Добавить тесты для UI sync-кнопки

### 10.3. Инструкция для команды
- [ ] Написать пошаговую инструкцию первой настройки:
  1. Установить скрипт в Tampermonkey
  2. Открыть /operations
  3. Нажать "Sync" → ввести настройки
  4. Проверить подключение
  5. Готово — дальше нажимать "Sync" после каждого сбора

---

## Порядок выполнения

```
Фаза 0 → Фаза 8.1 (метаданные) → Фаза 1 → Фаза 2 → Фаза 3 →
Фаза 4 → Фаза 5 → Фаза 6 → Фаза 7 → Фаза 8.2-8.3 →
Фаза 9 → Фаза 10
```

---

## Оценка объёма

| Компонент | Примерный размер |
|-----------|-----------------|
| GitHubSync модуль | ~250-350 строк |
| Merge логика | ~100-150 строк |
| UI (кнопка, диалог, прогресс) | ~150-200 строк |
| Настройки Sync в SettingsManager | ~50-80 строк |
| Рефакторинг ExportModule | ~30-50 строк |
| **Итого новый код** | **~580-830 строк** |
| **Размер файла после** | **~3700-3960 строк** |

---

## Риски и ограничения

| Риск | Вероятность | Митигация |
|------|------------|-----------|
| GitHub rate limit (5000 req/hr) | Низкая (ручной sync) | Показать ошибку, предложить подождать |
| Конфликт sha при параллельном sync | Н��зкая | Retry: повторный GET + merge + PUT |
| Большой размер JSON (100MB лимит GitHub) | Средняя при 20+ аккаунтах с годами | Каждый аккаунт — отдельный файл, мониторинг размера |
| Утечка токена | Низкая (GM_setValue) | Не логировать, не включать в экспорт |
| Потеря GM_setValue при сбросе профиля | Средняя | Инструкция: записать токен отдельно |
| CORS блокировка | Нулевая | GM_xmlhttpRequest обходит CORS |

---

## Что НЕ менять

- **OperationsCollector** — логика сбора не затрагивается
- **BetsDetailsFetcher** — логика загрузки деталей не меняется
- **XHRInterceptor** — перехват запросов без изменений
- **earlyInit** — раннняя инициализация без изменений
- **SiteDetector** — определение сайта без изменений
- **SegmentMapper** — маппинг сегментов без изменений
- **Существующая кнопка "Экспорт данных"** — остаётся для локального экспорта
