# TODO: Fonbet & Pari Collector v2.0.1

Мультисайтовый сбор данных с fon.bet и pari.ru на странице `/operations`.

---

## Статус проекта

| Фаза | Статус | Описание |
|------|--------|----------|
| Фаза 1 | ✅ ВЫПОЛНЕНО | Исправление BetsDetailsFetcher |
| Фаза 2 | ✅ ВЫПОЛНЕНО | Автоматизация процесса |
| Фаза 3 | ✅ ЧАСТИЧНО | Оптимизация (exponential backoff) |
| Фаза 4 | ✅ ВЫПОЛНЕНО | Чистка кода |
| Фаза 4.5 | ✅ ВЫПОЛНЕНО | Дополнительная оптимизация |
| Фаза 5 | ✅ ВЫПОЛНЕНО | Обновление экспорта |
| Фаза 6 | ✅ ВЫПОЛНЕНО | UI улучшения и панель настроек |
| Фаза 6.1 | ✅ ВЫПОЛНЕНО | Баг-фиксы и чистка кода |
| Фаза 6.2 | ✅ ВЫПОЛНЕНО | Критический баг-фикс |
| Фаза 6.3 | ✅ ВЫПОЛНЕНО | Исправление endpoint для пагинации |
| Фаза 6.4 | ✅ ВЫПОЛНЕНО | Исправление обработки пустых финальных ответов |
| Фаза 7 | ✅ ВЫПОЛНЕНО | Мультисайтовая поддержка (v2.0.0) |
| Фаза 8 | ✅ ВЫПОЛНЕНО | Cleanup мёртвого кода (v2.0.1) |
| Фаза 9 | ✅ ВЫПОЛНЕНО | GitHub Sync — синхронизация с GitHub (v2.1.0) |

---

## Ключевое открытие

**При клике на строку операции на `/operations` происходит запрос `coupon/info`!**

```
URL: https://clientsapi-lb52-w.bk6bba-resources.ru/coupon/info
Body: {"regId": <marker>, "lang": "ru", "betTypeName": "sport", "fsid": "...", ...}
```

**Важно:** `marker` операции = `regId` для запроса деталей!

---

## Фаза 1: Исправление BetsDetailsFetcher ✅

- [x] Использовать marker вместо regId
- [x] Обновить endpoint и параметры запроса
- [x] Тестирование (271 ставка, 100% success rate)

---

## Фаза 2: Автоматизация процесса ✅

- [x] Unified flow на странице `/operations`
- [x] Автозапуск OperationsCollector
- [x] Автоматический запуск BetsDetailsFetcher
- [x] Прогресс-индикация

---

## Фаза 3: Оптимизация ✅ ЧАСТИЧНО

- [x] Exponential backoff (INITIAL_RETRY_DELAY=500ms, MAX_RETRY_DELAY=8s, MULTIPLIER=2)
- [x] Логирование failed markers (failedMarkers[])
- [x] Graceful degradation
- [ ] Оптимальный BATCH_SIZE для прямых запросов (текущий: 5)

---

## Фаза 4: Чистка кода ✅ ВЫПОЛНЕНО (v1.15.0)

### Удалено:
- [x] AutoOpener (~713 строк) — удалён в v1.14.1
- [x] ProfileManager — удалён в v1.15.0
- [x] SettingsModule — удалён в v1.15.0
- [x] SettingsModal (~950 строк) — удалён в v1.15.0
- [x] MODE_PROFILES (yolo/stealth/superStealth) — удалён в v1.15.0
- [x] XHRInterceptor: coupon/info перехват — упрощён (только operations)
- [x] SELECTORS для DOM /bets (COUPON, COUPON_NUMBER, ROW_CONTAINER, SCROLL_CONTAINER)
- [x] URL_PATTERNS.COUPON_INFO
- [x] LIMITS: MOUSE_*, LOTTERY_ID_THRESHOLD, SCROLL_NEAR_BOTTOM_PX
- [x] Stealth-функции: checkProbability, randomInRange, getCouponId, smoothScrollBy, simulateMouseMovement, shouldTakeReadingPause, shouldPauseDuringScroll, getPauseDuration, getCurrentBatchSize
- [x] @match /bets — оставлен только /operations

### Размер файла после Фазы 4:
- До: ~5035 строк
- После: ~3213 строк
- Удалено: ~1822 строки (~36%)

---

## Фаза 4.5: Дополнительная оптимизация ✅ ВЫПОЛНЕНО

### Удалено из XHRInterceptor:
- [x] retryTimers Map
- [x] _handleLoad() — обработчик coupon/info ответов
- [x] _scheduleRetry(), _executeRetry() — логика retry
- [x] _retryClickByCouponId(), _retryClickByRegId() — клики для retry
- [x] _cancelAllRetries() — отмена retry
- [x] _markXHRResolved(), _validateData(), _isDuplicate(), _saveData() — helper функции
- [x] COUPON_INFO блок в _patchXHR()

### Удалено из UIPanel:
- [x] Mode slider (fc-mode-slider)
- [x] YOLO warning
- [x] Mode labels (YOLO, Stealth, Super, Custom)
- [x] Auto-opener checkbox
- [x] Settings кнопка
- [x] Статистика "Купонов кликнуто" и "Повторов"
- [x] _initializeModeSlider()
- [x] _handleModeChange()
- [x] _updateModeIndicator()
- [x] _updateModeLabels()
- [x] Mode-related CSS (~130 строк)

### Удалено из AppState:
- [x] clickCount, retryCount, isAutoOpenerRunning, currentMode

### Размер файла после Фазы 4.5:
- До: ~3213 строк
- После: ~2502 строк
- Удалено: ~711 строк (~22%)

### Итого удалено с v1.14.1:
- ~2533 строки (~50% от исходного размера)

---

## Фаза 5: Обновление экспорта ✅ ВЫПОЛНЕНО

### 5.1. JSON v2.1
- [x] detailsLoaded: число
- [x] detailsFailed: число
- [x] detailsSkipped: число (быстрые ставки без деталей)

### 5.2. Структура ставки с деталями
```json
{
  "marker": "2454979959",
  "status": "won",
  "operations": [...],
  "details": {
    "events": [...],
    "outcomes": [...],
    "coefficients": [...]
  }
}
```

---

## Известные ограничения

1. **Быстрые ставки (760, 764)** — детали недоступны через coupon/info
2. **Регрессионное тестирование fon.bet** — v2.0.1 ещё не протестирована на fon.bet

---

## Проверенные факты

| Факт | Значение |
|------|----------|
| Операции содержат regId? | ❌ НЕТ |
| Операции содержат marker? | ✅ ДА |
| marker = regId для coupon/info? | ✅ ДА |
| Клик на строку /operations вызывает coupon/info? | ✅ ДА |
| Можно делать прямой fetch coupon/info? | ✅ ДА |
| AutoOpener нужен? | ❌ НЕТ (удалён) |
| ProfileManager нужен? | ❌ НЕТ (удалён) |
| SettingsModal нужен? | ❌ НЕТ (удалён) |
| Mode slider нужен? | ❌ НЕТ (удалён) |
| XHR retry логика нужна? | ❌ НЕТ (удалён) |

### Рабочий endpoint и параметры

```javascript
// ✅ Динамический URL через SiteDetector
// Fonbet: https://clientsapi-lb52-w.bk6bba-resources.ru/coupon/info
// Pari:   https://clientsapi-lb01-w.pb06e2-resources.com/coupon/info
await fetch(SiteDetector.getCouponInfoUrl(), {
    method: 'POST',
    headers: { 'Content-Type': 'text/plain;charset=UTF-8' },
    body: JSON.stringify({
        regId: parseInt(marker),  // marker из операции
        lang: 'ru',
        betTypeName: 'sport',
        fsid: sessionParams.fsid,
        sysId: sessionParams.sysId || 21,
        clientId: sessionParams.clientId
    })
});
```

---

## Фаза 6.1: Баг-фиксы и чистка кода ✅ ВЫПОЛНЕНО (v1.15.2)

### Исправлено:
- [x] **UI счётчики операций показывали 0**
  - Причина: `getStats()` возвращал `total`, а код использовал `totalOperations`
  - Решение: переименован `total` → `totalOperations` для консистентности
  - Теперь счётчики обновляются корректно в реальном времени

- [x] **Удалена legacy кнопка "Export JSON"**
  - Старая кнопка экспортировала `state.collectedData` (из AutoOpener)
  - Оставлена только актуальная кнопка "💰 Экспорт данных"
  - Экспортирует операции из OperationsCollector в формате v2.1
  - Метод `exportJSON()` оставлен в коде (не используется)
  - Убран из консольного API `fonbetCollector.export()`

### Итого:
- Исправлены баги после v1.15.1
- Упрощён UI (3 кнопки вместо 4)
- Убрана путаница с двумя кнопками экспорта

---

## Фаза 6.2: Критический баг-фикс ✅ ВЫПОЛНЕНО (v1.15.3)

### Исправлено:
- [x] **Критический баг: UIPanel не инициализировался**
  - Причина: вызов удаленного метода `this._initializeModeSlider()` в строке 1390
  - Метод был удален в Фазе 4.5, но вызов остался
  - Ошибка: `TypeError: this._initializeModeSlider is not a function`
  - Следствие: UI панель не создавалась, скрипт не работал
  - Решение: удален вызов `this._initializeModeSlider()` из метода `create()`
  - Скрипт теперь корректно инициализируется и собирает данные

### Итого:
- Исправлен критический баг, блокирующий работу скрипта
- Версия обновлена до v1.15.3

---

## Фаза 6.3: Исправление endpoint для пагинации ✅ ВЫПОЛНЕНО (v1.15.4)

### Исправлено:
- [x] **Неправильный домен для запросов пагинации**
  - Проблема: endpoint использовал `.com` вместо `.ru` и `lb51` вместо `lb52`
  - Старый: `https://clientsapi-lb51-w.bk6bba-resources.com/session/client/prevOperations`
  - Новый: `https://clientsapi-lb52-w.bk6bba-resources.ru/session/client/prevOperations`
  - Следствие: запросы следующих порций операций возвращали пустой ответ
  - Решение: обновлен endpoint на правильный домен `.ru` и lb52

### Итого:
- Исправлен баг с пагинацией операций
- Скрипт теперь корректно загружает все операции в несколько запросов
- Версия обновлена до v1.15.4

---

## Фаза 6.4: Исправление обработки пустых финальных ответов ✅ ВЫПОЛНЕНО (v1.15.5)

### Исправлено:
- [x] **Скрипт "зависал" в режиме сбора на некоторых аккаунтах**
  - Проблема: При получении ответа `{operations: [], completed: true}` скрипт не завершал сбор
  - Причина: Логика обработки не учитывала случай "финальный ответ без операций"
  - Следствие: Скрипт логировал "⚠️ Получен пустой ответ, игнорируем" и продолжал работать
  - Решение: Добавлена отдельная ветка для `data.completed === true && operations.length === 0`
  - Теперь скрипт корректно завершает сбор при получении пустого финального ответа

### Итого:
- Исправлен критический баг, который приводил к зависанию сбора на некоторых аккаунтах
- Скрипт теперь корректно обрабатывает все варианты ответов пагинации
- Версия обновлена до v1.15.5

---

## Фаза 6: UI улучшения и панель настроек ✅ ВЫПОЛНЕНО (v1.15.1)

### 6.1. UI баги и улучшения
- [x] **Исправлено обновление UI счётчиков**
  - Обновлён метод `_getStateHash()` для включения статистики операций
  - Счётчики теперь обновляются автоматически при изменении данных

- [x] **Переименован "XHR собрано:" → "Операций собрано:"**
  - Обновлена логика для отображения количества собранных операций

- [x] **Сохранение статуса после завершения**
  - Добавлены поля `isCollectionCompleted` и `completionStats` в AppState
  - Прогресс-бар остаётся видимым после завершения
  - Статус показывает "✅ Сбор завершён: N операций, M групп"
  - Добавлен CSS стиль для completed статуса

- [x] **Панель настроек**
  - Создан модуль SettingsManager с функциями сохранения/загрузки
  - Добавлена кнопка "Настройки" (⚙️) в заголовок панели
  - Реализовано боковое выдвижное модальное окно с настройками:
    - Экспорт: пользовательский префикс, временная метка
    - Загрузка деталей: batch size, задержки, retry параметры
  - Настройки сохраняются через GM_setValue/GM_getValue
  - Кнопки "Сохранить" и "Сбросить к значениям по умолчанию"

### Размер файла после Фазы 6:
- До: ~2502 строки (v1.15.0)
- Посл��: ~3100 строк (v1.15.1)
- Добавлено: ~600 строк (+24%) - панель настроек и улучшения UI

---

## Следующие задачи

### Приоритет 1: Регрессионное тестирование
1. [ ] Протестировать v2.1.0 на fon.bet (регрессия)
2. [ ] Протестировать GitHubSync на fon.bet
3. [ ] Протестировать GitHubSync на pari.ru

### Приоритет 2: Подготовка инфраструктуры
1. [ ] Создать приватный GitHub-репозиторий `betting-data`
2. [ ] Создать fine-grained PAT с правами `Contents: Read and write`
3. [ ] Проверить работу токена через API

### Приоритет 3: Улучшения (опционально)
1. Улучшить логирование в режиме DEBUG_MODE
2. Добавить уведомления при завершении сбора (если разрешено)

---

## Фаза 7: Мультисайтовая поддержка ✅ ВЫПОЛНЕНО (v2.0.0)

### Реализовано:
- [x] Исследование API pari.ru — API на 100% совместимы
- [x] Модуль SiteDetector для автоопределения сайта
- [x] Добавлен `@match pari.ru/account/history/operations`
- [x] Динамические URL через SiteDetector (fallback API base, coupon/info)
- [x] PREV_OPERATIONS добавлен во все интерсепторы
- [x] Экспорт включает поле `site` и `siteId`
- [x] Глобальный объект: `window.collector`
- [x] Кэш: `_collectorCachedOperations`
- [x] Исправлен regId: fallback на `details.header.regId` || `marker`
- [x] UI заголовок с именем сайта

### Результаты тестирования (pari.ru):
- 97 ставок собрано
- 6 депозитов
- 97/97 деталей загружено (100%)
- 0 ошибок
- site: "Pari" в экспорте

### Файлы:
- `universal_collector_v2.0.0.user.js` — основной скрипт
- `fonbet_collector_v1_15_0.user.js` — предыдущая версия (архив)

---

## Фаза 8: Cleanup мёртвого кода ✅ ВЫПОЛНЕНО (v2.0.1)

### Удалено (~277 строк):
- [x] Мёртвый `ExportModule.exportJSON()` (76 строк)
- [x] Мёртвые поля AppState: `collectedData`, `collectedRegIds`, `xhrCount`, `errorCount`, `duplicateCount`, `startTime`
- [x] Мёртвые UI-элементы: "Ошибок", "Дубликатов", "Время" в HTML
- [x] Мёртвые функции: `getRegIdsForDetails()`, `sleep()`, `formatTime()`, `playCompletionSound()`
- [x] Мёртвый CSS: `.fc-stat-retry`, `.fc-modules`, `.fc-checkbox`, `.fc-btn-export`
- [x] DEBUG-логи из `_patchFetch()` и `_handleOperationsLoad()`
- [x] Мёртвый `XHRInterceptor.getStats()`
- [x] `SELECTORS` (от удалённой страницы /bets)
- [x] `LIMITS.MIN_DELAY_MS` (legacy от stealth-режима)
- [x] Мёртвые ссылки из `_cacheElements()`, `update()`, `_getStateHash()`, `_startAutoUpdate()`
- [x] `_updateTimer()` метод

### Исправлено:
- [x] Баг: `_updateButtons()` ссылался на несуществующее `isAutoOpenerRunning`
- [x] Устаревшие NOTE-комментарии и версионные теги

### Размер файла после Фазы 8:
- До: 3133 строки (v2.0.0)
- После: 2856 строк (v2.0.1)
- Удалено: 277 строк (~8.8%)

---

## Фаза 9: GitHub Sync ✅ ВЫПОЛНЕНО (v2.1.0)

Модуль инкрементальной синхронизации данных с GitHub. Подробный план — `TODO_v2.1_SYNC.md`.

### Реализовано:
- [x] Модуль GitHubSync (~560 строк): API, merge, sync, setup dialog, changeAlias
- [x] Обёртка GM_xmlhttpRequest для CORS-free GitHub API запросов
- [x] Merge-логика по ключу `marker` (local overwrites remote)
- [x] SHA-конфликт retry при параллельном sync
- [x] Диалог настройки с тестированием подключения
- [x] changeAlias() с безопасным переименованием файла в GitHub
- [x] Рефакторинг ExportModule: _buildExportData() — общий метод для экспорта и sync
- [x] UI: кнопка Sync, статус синхронизации, настройки sync в панели
- [x] Метаданные: @grant GM_xmlhttpRequest, @connect api.github.com
- [x] Консольные команды: collector.sync(), collector.changeAlias()

### Структура файлов в GitHub:
```
betting-data/
├── fonbet/
│   ├── 17158121_Vlad.json
│   └── 12345678_Sergey.json
└── pari/
    └── 98765432_Vlad.json
```

### Размер файла после Фазы 9:
- До: 2856 строк (v2.0.1)
- После: ~3549 строк (v2.1.0)
- Добавлено: ~693 строки (+24%)
